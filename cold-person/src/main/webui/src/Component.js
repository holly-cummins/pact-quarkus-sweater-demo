const Component = ({component}) => {
    return (

        <div className="component-card">
            <h2 className="component-colour">{component.name}</h2>
        </div>
    );
};

export default Component;
